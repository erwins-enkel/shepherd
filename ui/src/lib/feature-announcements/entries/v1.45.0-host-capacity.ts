import type { FeatureAnnouncement } from "../../feature-announcements";

const entry = {
  id: "host-capacity",
  sinceVersion: "1.45.0",
  titleKey: "feat_host_capacity_title",
  bodyKey: "feat_host_capacity_body",
} satisfies FeatureAnnouncement;

export default entry;
