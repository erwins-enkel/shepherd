import type { FeatureAnnouncement } from "../../feature-announcements";

const entry = {
  id: "decommission-command",
  sinceVersion: "1.44.0",
  titleKey: "feat_decommission_command_title",
  bodyKey: "feat_decommission_command_body",
} satisfies FeatureAnnouncement;

export default entry;
