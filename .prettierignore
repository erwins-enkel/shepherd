# Lockfiles
bun.lock

# Self-contained Astro marketing site (own toolchain; later extracted to its own repo)
site

# Astro Starlight docs site (own toolchain; .astro/.md formatted by astro, not root prettier)
docs-site

# Generated changelog — leave its formatting alone
CHANGELOG.md

# Build outputs
node_modules
.svelte-kit
build
dist

# Prettier + eslint cache dirs (--cache-location .cache/...). prettier honors this
# file, not .gitignore, so without this `prettier --check .` traverses & fails on the
# restored cache JSON in CI (#1192). Bare `.cache` matches root + extension/.cache.
.cache

# Extension build artifacts copied into public/ (axe-core bundle + esbuild-built recorder/picker)
extension/public/axe.min.js
extension/public/recorder.js
extension/public/picker.js

# Transient agent worktrees (nested checkouts; not project source)
.claude/worktrees

# Agent-generated planning artifacts — leave their formatting alone
docs/superpowers

# Intentionally-malformed JSON fixture (#822 regression: unescaped inner quotes — must not parse)
test/fixtures/recap-task561.json

# Paraglide generated output + vendored inlang plugins/metadata
ui/src/lib/paraglide
ui/project.inlang/.meta.json
ui/project.inlang/README.md
ui/project.inlang/plugins
extension/src/lib/paraglide
extension/project.inlang/.meta.json
extension/project.inlang/README.md
extension/project.inlang/plugins

# Spike #1043 raw terminal-frame fixtures (byte-exact captures, not formatted source)
test/fixtures/fullscreen
