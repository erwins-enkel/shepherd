import type { FeatureAnnouncement } from "../../feature-announcements";

const entry = {
  id: "herdr-sandbox-idle-advisory",
  sinceVersion: "1.45.0",
  titleKey: "feat_herdr_sandbox_advisory_title",
  bodyKey: "feat_herdr_sandbox_advisory_body",
} satisfies FeatureAnnouncement;

export default entry;
