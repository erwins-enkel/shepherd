import type { FeatureAnnouncement } from "../../feature-announcements";

const entry = {
  id: "newtask-keymap",
  sinceVersion: "1.47.0",
  titleKey: "feat_newtask_keymap_title",
  bodyKey: "feat_newtask_keymap_body",
} satisfies FeatureAnnouncement;

export default entry;
