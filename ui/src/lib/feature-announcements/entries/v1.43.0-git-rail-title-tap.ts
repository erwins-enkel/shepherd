import type { FeatureAnnouncement } from "../../feature-announcements";

const entry = {
  id: "git-rail-title-tap",
  sinceVersion: "1.43.0",
  titleKey: "feat_title_tap_toggle_title",
  bodyKey: "feat_title_tap_toggle_body",
} satisfies FeatureAnnouncement;

export default entry;
