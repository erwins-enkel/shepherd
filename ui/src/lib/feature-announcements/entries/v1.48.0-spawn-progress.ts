import type { FeatureAnnouncement } from "../../feature-announcements";

const entry = {
  id: "spawn-progress",
  sinceVersion: "1.48.0",
  titleKey: "feat_spawn_progress_title",
  bodyKey: "feat_spawn_progress_body",
} satisfies FeatureAnnouncement;

export default entry;
