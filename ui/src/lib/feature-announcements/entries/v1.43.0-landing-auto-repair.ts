import type { FeatureAnnouncement } from "../../feature-announcements";

const entry = {
  id: "landing-auto-repair",
  sinceVersion: "1.43.0",
  titleKey: "feat_landing_auto_repair_title",
  bodyKey: "feat_landing_auto_repair_body",
} satisfies FeatureAnnouncement;

export default entry;
