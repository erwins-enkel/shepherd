import type { FeatureAnnouncement } from "../../feature-announcements";

const entry = {
  id: "per-role-effort",
  sinceVersion: "1.42.0",
  titleKey: "feat_per_role_effort_title",
  bodyKey: "feat_per_role_effort_body",
} satisfies FeatureAnnouncement;

export default entry;
